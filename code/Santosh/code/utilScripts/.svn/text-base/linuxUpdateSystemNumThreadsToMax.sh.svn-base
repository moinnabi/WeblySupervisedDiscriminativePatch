#!/bin/bash

# run it as . thisFilename.sh
# the dot is important so that the shell script is not spawned in a child

ulimit -u
maxTh=`ulimit -Hu`
#echo $maxTh
ulimit -u $maxTh
#ulimit -u 20000
ulimit -a
